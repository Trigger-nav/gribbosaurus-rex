"""DEPRECATED — use gribbosaurus_rex.extract.latest_point_forecasts.

Kept only so old imports fail loudly with a pointer instead of silently
returning API data that isn't GRIB-derived."""

raise ImportError(
    "gribbosaurus_rex.ingest.point is deprecated — use gribbosaurus_rex.extract")
